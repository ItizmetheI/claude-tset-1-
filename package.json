{
  "name": "portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.13",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.1",
    "vite": "^6.3.5",
    "wrangler": "^4.99.0"
  }
}
